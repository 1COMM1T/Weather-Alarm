spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/commit
    username: commit
    password: commit
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    generate-ddl: false
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true
    database: mysql

#aws:
#  s3:
#    access-key: YOUR_ACCESS_KEY
#    secret-key: YOUR_SECRET_KEY
#    region: YOUR_AWS_REGION
#    bucket: YOUR_BUCKET_NAME

server:
  port: 8080